# Serialized model artifacts

This directory is reserved for saved model files produced by the optional save helpers in the application modules. Include only the artifacts needed for the final demonstration; do not commit large temporary training outputs.
