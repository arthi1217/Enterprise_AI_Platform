# Static assets

Place reusable interface assets, architecture visuals, and presentation-ready diagrams here. The Streamlit interface is currently styled through `app.py` and `.streamlit/config.toml`.
